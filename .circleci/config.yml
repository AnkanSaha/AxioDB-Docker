version: 2.1

executors:
  docker-executor:
    docker:
      - image: docker:20.10.7  # Specify Docker image (you can change this to any version you prefer)
    working_directory: ~/repo

jobs:
  checkout:
    docker:
      - image: docker:20.10.7
    steps:
      - checkout  # Checkout the code from the repository

  build:
    docker:
      - image: docker:20.10.7
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.7
          docker_layer_caching: true
      - run:
          name: Set up Docker Buildx
          command: |
            docker buildx create --use
      - run:
          name: Build and push Docker image
          command: |
            docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
            docker build -t $DOCKER_REGISTRY/$DOCKER_USERNAME/$DOCKER_IMAGE:$CIRCLE_SHA1 .
            docker push $DOCKER_REGISTRY/$DOCKER_USERNAME/$DOCKER_IMAGE:$CIRCLE_SHA1

workflows:
  version: 2
  build_and_push:
    jobs:
      - build:
          filters:
            branches:
              only:
                - main
